/**
 * Created by hmchung on 5/9/2015.
 */
describe( 'home section', function() {
  beforeEach( module( 'module.home' ) );

  it( 'should have a dummy test', inject( function() {
    expect( true ).toBeTruthy();
  }));
});